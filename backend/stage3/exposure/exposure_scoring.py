"""Exposure scoring — T3.3.

Not yet implemented.

Will implement `compute_exposure_score(footprint_or_segment,
population_density: Optional[float] = None) -> float`. Per T3.3's
anti-hallucination rule: if no real, confirmed population-density source
exists, must work correctly with population_density=None — never
fabricate a population figure.
"""
