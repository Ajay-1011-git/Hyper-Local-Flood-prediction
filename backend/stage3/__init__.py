"""Stage 3: damage risk layer (hazard × exposure × vulnerability).

See CLAUDE.md (this directory) before starting any task — in particular
its "STOP" section on the unconfirmed NodeState/SimulationResult contract.
"""
