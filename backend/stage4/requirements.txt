# Versions pinned to latest stable on PyPI as of 2026-08-20 (confirmed
# in-session earlier the same day, reused here rather than re-checked --
# per this project's convention of resolving real versions, not guessing).
fastapi==0.141.1
pydantic==2.13.4
pydantic-settings==2.15.0
sqlalchemy==2.0.52
asyncpg==0.31.0
greenlet==3.5.5                # required by SQLAlchemy's async engine
redis==8.1.0
requests==2.34.2
httpx==0.28.1                  # required by FastAPI's TestClient / AsyncClient
uvicorn==0.52.4

pytest==9.1.1
pytest-asyncio==1.4.0
mypy==2.3.1

# T4A.1's XML library: deliberately NOT pinned here yet -- the task's own
# scope is to confirm the right one (e.g. lxml) in-session before writing
# any CAP-XML generation code, not assume it at scaffolding time.
