"""TN WRD rainfall telemetry client — T1B.4.

Not yet implemented. `TNWRD_DATASET_URL` (see config.py) is confirmed to
exist as a real dataset page, but its CSV column structure has NOT been
confirmed — fetch it directly in-session and inspect real column names
before writing any parsing code that assumes a schema.
"""
