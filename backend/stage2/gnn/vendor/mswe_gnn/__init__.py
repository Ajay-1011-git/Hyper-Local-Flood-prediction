"""Vendored subset of RBTV1/mSWE-GNN (MIT License).

Fetched directly from github.com/RBTV1/mSWE-GNN in-session (2026-08-20).
See LICENSE in this directory. Only `models.py` (byte-identical to the
original) and a trimmed `gnn.py` (MSGNN class removed -- see that file's
own header) are vendored; nothing else from the original repo is needed
for the single-scale GNN(type_GNN="SWEGNN") architecture this project uses.
"""
