"""Database & Redis connection layer — T1B.1.

Not yet implemented. Will implement `get_db_session()` and
`get_redis_client()`, and create the `downscaled_forecast_field`,
`sensor_reading`, and `dem_metadata` tables.
"""
