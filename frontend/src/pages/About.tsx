/**
 * About / Methodology (`/about`) — PLACEHOLDER, not yet T4C.6.
 *
 * Exists now only so Landing's real footer link (T4C.0) lands somewhere
 * honest. T4C.6's real requirement is substantial (every honesty
 * disclosure this project makes — DEM interpolation, sensor
 * assimilation vs. forecast improvement, the vulnerability curve, which
 * forecast source is live) and deliberately NOT faked here with filler
 * text.
 */

import ComingSoonPage from './ComingSoonPage'

export function About() {
  return (
    <ComingSoonPage
      title="About & Methodology"
      note="This page isn't built yet — check back soon."
    />
  )
}

export default About
