"""Risk ranking — T3.5.

Not yet implemented.

Will implement `rank_structures(sim_result: SimulationResult, footprints:
List[BuildingFootprint], road_segments: List[RoadSegment]) ->
List[DamageRankEntry]`. Must be deterministic — identical input must
produce a byte-identical output, including tie-breaking order.
"""
