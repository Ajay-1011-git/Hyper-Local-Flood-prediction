"""CAP-XML generator — T4A.1, not yet implemented.

Will implement `generate_cap_xml(damage_ranking, sim_result, site_polygon)
-> str` once the real, current CAP schema and India's SACHET-specific
field usage are confirmed in-session (per this project's anti-hallucination
rule 1 -- never write XML generation code against an assumed schema).
"""
