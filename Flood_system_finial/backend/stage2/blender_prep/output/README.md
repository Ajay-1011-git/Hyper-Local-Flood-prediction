# Blender MCP deliverable — not present yet

`vit_vellore_site.glb` and `anchor_point.json` land here once the 3D model
(built in a separate system, per the user, 2026-08-20) is ready. Neither
file is committed to git (see the root `.gitignore`).

Until then, `ingestion/glb_loader.py`'s real VERIFY step (T2.1) stays
explicitly marked BLOCKED — not faked against a synthetic stand-in.
