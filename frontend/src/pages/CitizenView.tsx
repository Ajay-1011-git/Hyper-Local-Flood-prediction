/**
 * Citizen Alert View (`/citizen`) — PLACEHOLDER, not yet T4C.4.
 *
 * Exists now only so Landing's real "Check my area" link (T4C.0) lands
 * somewhere honest. T4C.4 will replace this file's content with the
 * real light, high-contrast citizen register (User Flow §3.5).
 */

import ComingSoonPage from './ComingSoonPage'

export function CitizenView() {
  return (
    <ComingSoonPage
      title="Citizen View"
      note="The citizen alert view isn't built yet — check back soon."
    />
  )
}

export default CitizenView
