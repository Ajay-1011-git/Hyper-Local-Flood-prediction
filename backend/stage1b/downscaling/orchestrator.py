"""Downscaling orchestration: RegionalEnsembleForecast -> DownscaledForecastField — T1B.8.

Not yet implemented. Depends on T1B.7. For development before Stage 1A's
live endpoint exists, use a clearly-labeled mock `RegionalEnsembleForecast`
fixture — never a fabricated "real" call.
"""
